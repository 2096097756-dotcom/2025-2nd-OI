#include<stdio.h>
/*
标准数组表示法: a[i][j]
行指针解引用 (使用 a[i]): *(a[i] + j)
解释： a[i] 是第 i 行的起始地址（一个指向整型的指针），加上偏移量 j 后再解引用。
数组名行偏移和列偏移 (使用 a): *(*(a + i) + j)
解释： (a + i) 是指向第 i 行的地址，对其解引用 *(a + i) 得到第 i 行本身（又退化为指向该行首元素的指针），再加上偏移量 j 后再解引用，即得到元素的值。
*/
#define N 20

int a[N],*p;
int main(){
    /*
    
    Model
    这是一个非常关键且常让人混淆的 C 语言二维数组和指针的知识点。下面我将详细解释 *(*(a + i) + j) 如何访问二维数组 a[i][j] 的元素：
    假设我们有一个二维数组声明：int a[M][N];，例如黑板上的 int a[2][3];，其中 M=2（行数），N=3（列数）。
    1. a：数组名（退化为行指针）
    a 是整个二维数组的名字。
    在表达式中（除了 sizeof 和 & 运算符），a 会退化为一个指向其首元素的指针。
    对于二维数组来说，它的首元素是第一行 a[0]。
    因此，a 的类型是 int (*)[N]，即 "指向一个包含 N 个整数的一维数组的指针"（行指针）。
    它的值是整个数组第一行（a[0]）的地址。
    2. (a + i)：第 i 行的地址
    指针算术： a + i 涉及指针算术。由于 a 是一个指向包含 N 个 int 的数组的指针，a + i 意味着将地址 a 向后移动 i 个完整的一维数组（即 i 行）。
    结果： (a + i) 的值是数组 a 中第 i 行（即 a[i]）的起始地址。
    类型： 它的类型仍然是 int (*)[N]（行指针）。
    3. *(a + i)：第 i 行的数组名（退化为指向首元素的指针）
    解引用操作： 对一个地址 (a + i) 进行解引用 * 操作。
    结果： *(a + i) 的值是第 i 行这个一维数组本身，即 a[i]。
    退化： 像任何数组名一样，a[i] 在表达式中会立即退化为指向其首元素的指针。
    类型： 因此，*(a + i) 的类型是 int *，即 "指向一个整型变量的指针"。它的值是 a[i][0] 的地址。
    4. (*(a + i) + j)：元素 a[i][j] 的地址
    指针算术： 现在 *(a + i) 是一个 int * 类型的指针，指向 a[i][0]。
    对一个 int * 类型的指针进行加 j 的操作，意味着将地址向后移动 j 个整型数据（因为指针是 int * 类型）。
    结果： (*(a + i) + j) 的值是数组元素 a[i][j] 的地址。
    类型： 它的类型是 int *。
    5. *(*(a + i) + j)：元素 a[i][j] 的值
    最终解引用： 对地址 (*(a + i) + j) 进行最终的解引用 * 操作。
    结果： 得到存储在地址 (*(a + i) + j) 处的值，这个值就是 a[i][j] 的元素值。
    类型： 它的类型是 int。
    总结等价关系
    表达式	描述	类型
    a[i][j]	数组元素访问的标准写法	int
    *(a[i] + j)	使用一维数组名和偏移	int
    *(*(a + i) + j)	使用行指针和双重解引用	int
    *(*(arr + l) + m) + n    访问三维数组元素	int
    *(arr+i)[j] ,arr+i 是一个一维数组名,行指针->*(arr+i)指的就是arr[i]，退化为指向首元素的指针，再加j偏移，解引用得到值
    
    *(arr[l][m]+n) arr[l][m]是一个一维数组名，退化为指向首元素的指针，再加n偏移，解引用得到值
    *(*(arr[l]+m)+n) arr[l]是一个二维数组名，退化为指向首元素的指针，加m偏移得到一维数组名，再加n偏移，解引用得到值
    面指针、行指针、元素指针
    */

}